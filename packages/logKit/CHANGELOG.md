# Changelog

## 0.2.0 — 2026-09-25

Two defects in persisted levels a read-only audit found, and the fixes.

- Fixed: an addon named `"*"` overwrote the saved global level. `BindLevels` keeps the global level under the key `"*"` of `global.logLevels`, and `ForAddon("*"):SetLevel(...)` wrote the same key, which the next `BindLevels` restored as the global level. `ForAddon` and `History` refuse the name at the caller: `LogKit:ForAddon addonName must not be "*", which names the global level`. In `/log` the `*` target was already the global level; `/log show *` now prints the global line too.
- Fixed: a level set from `/log` for a typo or a removed addon was saved for good, and the documented section (`max = 64`) could fill until later real levels applied only for the session. `/log <addon> <level>` for a name that has no logger now asks the client's `C_AddOns.DoesAddOnExist` and refuses a name it says is not installed (`/log: no logger or installed addon is named "Typo"; nothing was set`); an installed addon that has not created its logger yet is accepted as before, and so is any name when the client cannot tell (no such function, a call that raises, an answer that is not a plain boolean). `/log show` lists, after the loggers, every level set for a name without a logger (`Removed: trace (addon, no logger)`).
- Added: `/log clear [addon|*]`. With a name it removes that name's level from the session and its saved entry, even an entry `BindLevels` ignored because its value is no level name; with `*` it clears the global level; alone it removes every addon level, saved ones included, and prints `cleared <n> addon levels`. A `/log` registered by revision 3 or older (registered once per session) reaches the same code through its top-level handler. Saved entries are never pruned automatically: a saved name may be a logger created later, or an addon that is only disabled.
- Implementation revision 4. The state layout is unchanged, so an in-place upgrade adopts any older state as it is; `dispatch.commandClear` is new. A logger an older copy created for `"*"` keeps working for the session.
- `docs/API.md`: implementation revision 4, `C_AddOns.DoesAddOnExist` among the host facilities, the `"*"` refusal, the `/log` rules, `show *` and `clear`, and the upgrade note; `docs/INTERNALS.md` describes `isInstalledAddon`, `forgetAddonLevel`, the `clear` routing and why nothing is pruned at bind; the README follows.
- Specs: new `SavedLevels_spec.lua` (`"*"` refused by `ForAddon` and `History` with the saved global level kept across a rebind; a typo refused and never saved while an installed addon and a logger are accepted; any name accepted when `DoesAddOnExist` is missing, raises or answers a non-boolean or a secret; `/log show` with names without a logger and `/log show *`; `/log clear <addon>`, `/log clear` and `/log clear *` in the session and the saved section, entries with no level name included; room kept in a full section; `clear` through the top-level handler); the test environment gains `InstallAddOns`. 158 specs (150 before).
- The real-client suite: the `/log` test for a name without a logger now logs the client's `C_AddOns.DoesAddOnExist` answers, expects the typed-only name to be refused, sets a level for the installed harness `MoltenCodesTest` instead, and checks `/log show`, `/log show *` and `/log clear`. `EXPECTED.md` still expects 38 tests.

## 0.1.4 — 2026-09-25

- Documentation: `docs/API.md` (formatting, step 3) no longer gives "a `%d` given a string" as a sure format failure. What fails is the host `string.format`'s decision: LogKit reports whatever it raises and delivers whatever it formats. Lua 5.1 refuses `%d` given a string that is not a number, but in the Retail 12.1.0 (build 69933) run of 2026-09-25 LogKit reported nothing for `Warn("%d frames", "many")` or for a secret given to `%d` (its placeholder is such a string). The same client refuses a width over two digits (`%100s`). Documentation only: the implementation revision is unchanged.
- Real-client suite: the format-failure test uses `%100s`, which the client refuses, and expects the report to carry the client's own message. A new test logs what the client's `string.format` does with `%d` and `'many'` and expects LogKit to follow it, and the secret `%d` test compares LogKit's outcome with the client's answer for `'<secret>'`. `EXPECTED.md` expects 38 passed.

## 0.1.3 — 2026-09-24

- Secret values: API.md (argument errors) and the source comment on the facade check no longer claim that comparing a secret with anything, `nil` included, raises, or that a raw identity test is safe whatever the other side. They state what was measured on Retail 12.1.0 b69933 (2026-09-24): a secret compared with a value of its own type raises (`==`, `~=`, `<`, `<=` and `rawequal` alike) and a secret used as a table key raises, while a comparison with `nil` or with a value of another type answers without raising. The `type(value) == "nil"` rule stays, as the repository's uniform rule that never compares anything. Comments and documentation only: `luac -s -l` gives the same instruction listing before and after, so the implementation revision is unchanged.

## 0.1.2 — 2026-09-24

- Implementation revision 3, applying the repository rule for values LogKit did not create: their absence is tested with `type`, never with `== nil`, and a secret is refused before it is compared. `SetLimits` no longer compares a limit key with `nil` before asking `issecretvalue` (a secret key is still refused with `LogKit:SetLimits limits must not have a secret key`). The facade check tests the receiver's type before comparing it with the facade, so a secret receiver raises `LogKit:<Method> must be called on the LogKit facade; use LogKit:<Method>(...)` at the caller's line instead of raising inside LogKit. `/log` and `/log show` answer a secret addon name or level word with the usage and change nothing. `BindLevels` skips a secret saved level name, and checks that the database's `Pairs` is a function before comparing it. `SetLevel`, `SetGlobalLevel`, `History`, `ChatSink`, `BindLevels` and `SetLimits` test an omitted argument or field with `type`. Behaviour for every non-secret value is unchanged.
- The state layout is unchanged; a new bootstrap spec loads revision 2 (`REVISION - 1`), fills its state and checks that revision 3 takes it over in place. The upgrade specs load the next revision as `REVISION + 1`.
- New specs: a secret receiver, a secret slash-command token, a secret saved level name, and the upgrade from the previous revision. 150 specs.

## 0.1.1 — 2026-09-24

- Fixed `SetLimits` comparing a secret limit value (and indexing with a secret key) before asking `issecretvalue`, which raised inside LogKit on a client with secret values. It now refuses both at the caller's line, as every other argument is refused: `LogKit:SetLimits limits must not have a secret key`, `LogKit:SetLimits limits.<name> must not be a secret value`.
- Implementation revision 2. The state layout is unchanged; a new bootstrap spec loads revision 1, fills its state (a logger with an override, the global level, a sink, a journal entry, a raised limit) and checks that revision 2 takes it over in place.
- `docs/API.md`: `RegisterCommand` also passes CommandKit's `"full"` through, now listed with the refusal reasons; the error list names the secret refusals of `History` and of every level argument.
- Source comments: `callSinks` and `writeThrough` each carry their own description again, and `deliver` names what its `pcall` actually guards against.
- 146 specs.

## 0.1.0 — 2026-09-24

- Added LogKit API generation 1, implementation revision 1: levelled logging with per-addon loggers, lazy secret-safe formatting, a tri-state level, a bounded journal built on SignalKit's journal, and sinks.
- Added `LogKit.LEVELS`, a read-only table of the level ordering (`trace` 1, `debug` 2, `info` 3, `warn` 4, `error` 5, `off` 6), and `LogKit.DEFAULT_LEVEL` (`"warn"`).
- Added `LogKit:ForAddon(addonName)`, one memoised logger per addon name, bounded by `maxLoggers` (256; `nil, "capped"` beyond it).
- Added the logger methods `Trace`, `Debug`, `Info`, `Warn` and `Error(message, ...)`, plus `Log(level, message, ...)`, `IsEnabled(level)`, `SetLevel(level|nil)`, `GetLevel()` (the effective level and its source, `"addon"`, `"global"` or `"default"`) and `GetAddonName()`. A disabled call costs a receiver check and one comparison and never reads the message or its arguments; an enabled call formats with `string.format` only when arguments are present, so a bare message keeps its `%` characters. Arguments that are not strings or numbers are converted with `tostring` before `string.format`, so `%s` formats `nil`, booleans and tables as Lua 5.2 does.
- Format arguments that are secret values are replaced by `LogKit.SECRET_PLACEHOLDER` (`"<secret>"`) before formatting; a secret message, addon name, level, sink or chat frame is refused at the caller; `RemoveSink` answers `false` for a secret handle like any unknown value. A bad format string is reported through the host error handler and the message is dropped; the call never raises. At most `MAX_FORMAT_ARGUMENTS` (16) arguments per call.
- Added `LogKit:SetGlobalLevel(level|nil)` and `LogKit:GetGlobalLevel()`. Precedence: addon override, then global level, then the default. A setter repeating the level in force changes nothing and writes nothing.
- Added `LogKit:AddSink(sink)` for a function `(record)` or a table with `Write(record)`, returning a handle for `LogKit:RemoveSink(handle)`; bounded by `maxSinks` (16; `nil, "full"` beyond it). The record `{ addon, level, levelName, message, time }` is one reused table, valid during the call. A table sink's `Write` is looked up at every call. A failing sink is reported through the host error handler and the other sinks still run; an error handler that raises falls back to `print` and never reaches the logging caller; a message logged from inside a sink is journaled but not delivered to sinks. A journal firing SignalKit refuses (its `maxJournalArguments` lowered below 4) is reported and the message still reaches the sinks.
- Added `LogKit:ChatSink(chatFrame?)`, printing `[addon] level: message` with the level coloured through `chatFrame:AddMessage`, `DEFAULT_CHAT_FRAME` read per line, or `print`.
- Added `LogKit:History(addonName?, minimumLevel?)`, an allocation-free walk over the journal oldest to newest yielding `(position, addon, levelName, message, time)`, filtered by addon and level.
- Added `LogKit:RegisterCommand()`, registering `/log <addon|*> <level|default>` and `/log show [addon]` through CommandKit API 1 when it is loaded (`false, "absent"` otherwise, `false, "unavailable"` without `SlashCmdList`). The level word is read without case, extra tokens are ignored, and setting an addon's level never creates its logger.
- Added `LogKit:BindLevels(db)`, persisting addon overrides and the global level in a SettingsKit API 1 database whose `global` scope declares `logLevels`, restoring them on bind; `BindLevels(nil)` stops persisting. Without SettingsKit it refuses at the caller.
- Added `LogKit:SetLimits(limits)` and `LogKit:GetLimits()` over `journalCapacity` (1024, an integer up to 65536 and at most SignalKit's `maxJournalCapacity`; `UNBOUNDED` refused because the ring is preallocated; changing it re-creates the journal keeping the newest entries that fit), `maxSinks` (16), `maxMessageLength` (1024 bytes, at least 16; longer messages are cut and marked with `...`, never inside a UTF-8 sequence) and `maxLoggers` (256); the last three accept `LogKit.UNBOUNDED`. Values are validated at the caller before anything changes; the limits are shared by every consumer in the session.
- Records are stamped with `GetTimePreciseSec()`; on a host without it `time` is `false`.
- Package state, loggers, levels, sinks, the journal, `LEVELS` and the `UNBOUNDED` sentinel survive an in-place upgrade through `Registry:Bootstrap`; slash-command handlers dispatch through package state, so a newer revision replaces their behaviour.
- 144 specs: level gating, lazy formatting (a metatable-driven probe proves the arguments are never touched while disabled), bare messages with `%`, override precedence and `GetLevel` sources, journal ring order and filtering (nested walks, logging during a walk), sinks (function, table with `Write` looked up per call, removal of itself or another sink during a message, failing sink isolation, a raising error handler, a refused journal firing), the chat sink's output and colours, the secret placeholder, truncation, limits, the slash command (case, extra tokens, no logger created), the SettingsKit binding (one write per change), in-place upgrades, manifest agreement, error levels pinned for every argument failure, and allocation guards on a disabled call, an enabled call with a sink, and a history walk.
